"""Agent validator — première passe de validation (sous-paquet).

`agent.py` : ValidatorAgent — cohérence des nets, ERC statique (adaptateur
erc_executor), empreintes contre la bibliothèque → ValidationReport.
"""
