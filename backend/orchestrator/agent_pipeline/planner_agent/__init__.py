"""Agent planner — décomposition du cahier des charges (sous-paquet).

`agent.py` : PlanDocument + PlannerAgent (blocs fonctionnels, zones de
placement, budget de couches, contraintes via constraint_extractor).
"""
