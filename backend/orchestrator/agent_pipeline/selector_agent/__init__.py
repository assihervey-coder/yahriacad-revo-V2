"""Agent selector — validation de la short-list (sous-paquet).

`agent.py` : SelectorAgent — compatibilité électrique/empreinte, arbitrage
coût/performances/stock, BOM initial (list[Component]) + justifications.
"""
