"""Sous-module nl_to_skidl — langage naturel vers script SKiDL (brique Circuitron).

Bibliothèque de blocs fonctionnels paramétriques (templates.py) + analyse par
mots-clés déterministe (nl_to_skidl.py), avec hook LLM optionnel (jamais requis).
"""
